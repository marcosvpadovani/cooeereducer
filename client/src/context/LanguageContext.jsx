import React, { createContext, useState } from "react";

const ptBr = {
  headers: {},
};

const LanguageContext = createContext();

const LanguageProvider = () => {
  return <div>LanguageProvider</div>;
};

export default LanguageProvider;
